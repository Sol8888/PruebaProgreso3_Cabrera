<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PruebaProgreso3_Cabrera.Views.BuscadorAeropuertosPage"
             Title="BuscadorAeropuertosPage">
    <VerticalStackLayout Padding="10">
        <Entry x:Name="scabrera_txtSearch" Placeholder="Ingresa un país..." />
        <Button Text="Buscar" Command="{Binding ComandoBuscar}" />
        <Button Text="Limpiar" Command="{Binding ComandoLimpiar}" />
        <Label Text="{Binding Mensaje}" />
    </VerticalStackLayout>
</ContentPage>